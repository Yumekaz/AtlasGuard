## Review Issues

### Issue 1 — Severity: bug
- **File**: apps/api/src/admin/admin.controller.ts:67
- **Description**: simulate-demo uses responderProfile.updateMany with no where, moving all responders to (27.31, 88.58).
- **Suggestion**: Limit to demo responder, e.g. where: { user: { email: 'responder@demo.com' } }.
- **Status**: fixed
- **Response**: Moved responder repositioning into `prepareDemoScenario()` with `where: { user: { email: 'responder@demo.com' } }` so only the demo unit is relocated.

### Issue 2 — Severity: bug
- **File**: apps/api/src/operator/operator.service.ts:72
- **Description**: averageResponseTimeMinutes uses all-time ACKNOWLEDGED events while resolvedToday is today-only; lifetime average can mislead during demos.
- **Suggestion**: Scope to today/recent window or per-incident first ACK.
- **Status**: fixed
- **Response**: Scoped ACKNOWLEDGED events to `createdAt >= startOfToday` and compute average from each incident's first acknowledge event only.

### Issue 3 — Severity: bug
- **File**: apps/web/src/app/dashboard/operator/page.tsx:122
- **Description**: WebSocket updates refresh the queue but not loadSummary(); activeBySeverity goes stale.
- **Suggestion**: Re-fetch summary on socket events (debounced) or derive severity counts from live incidents.
- **Status**: fixed
- **Response**: Added debounced (500ms) `scheduleSummaryRefresh()` called from socket `onCreated`/`onUpdated` handlers.

### Issue 4 — Severity: suggestion
- **File**: apps/web/src/app/dashboard/operator/page.tsx:62
- **Description**: loadSummary swallows errors silently.
- **Suggestion**: Show a non-blocking warning when summary fetch fails.
- **Status**: fixed
- **Response**: Added `summaryWarning` state and a non-blocking `alert-warning` banner when summary fetch fails.

### Issue 5 — Severity: suggestion
- **File**: packages/shared/src/risk-scoring.test.ts:47
- **Description**: "caps at 100" test asserts 90; max with current rules is 90, so Math.min(score, 100) is never tested.
- **Suggestion**: Add an explicit cap test or document max score.
- **Status**: fixed
- **Response**: Exported `capRiskScore`, `RISK_SCORE_CAP`, and `MAX_ACCUMULATED_RULE_SCORE` (90). Renamed stacking test and added explicit `capRiskScore(110) === 100` test.

### Issue 6 — Severity: suggestion
- **File**: packages/shared/src/risk-scoring.test.ts
- **Description**: No boundary tests at exactly 5.0 km or exactly 3 nearby incidents (> thresholds).
- **Suggestion**: Add Vitest cases at 5.0 km and count 3.
- **Status**: fixed
- **Response**: Added boundary tests confirming 5.0 km and count 3 do not trigger their respective rules (thresholds are strictly >5 and >3).

### Issue 7 — Severity: suggestion
- **File**: apps/web/src/components/RiskExplanationPanel.tsx:18
- **Description**: Malformed JSON fails safely but shows no fallback when reasons are missing.
- **Suggestion**: Show "Risk breakdown unavailable" when parse fails but riskExplanation is present.
- **Status**: fixed
- **Response**: `parseExplanation` now tracks `parseFailed`; panel shows "Risk breakdown unavailable" when JSON is present but invalid.

### Issue 8 — Severity: suggestion
- **File**: apps/api/src/risk-scoring/geo.utils.ts:19
- **Description**: isNightTime uses server local time, not incident timezone (IST/Sikkim).
- **Suggestion**: Use Asia/Kolkata or location-based offset.
- **Status**: fixed
- **Response**: `isNightTime` now derives hour via `Intl.DateTimeFormat` with `timeZone: 'Asia/Kolkata'`.

### Issue 9 — Severity: suggestion
- **File**: apps/api/src/admin/seed-demo.helper.ts:122
- **Description**: Demo cancel events use fake hashes, breaking audit chain integrity post-simulate.
- **Suggestion**: Use IncidentEventsService.appendEvent or document demo exception.
- **Status**: fixed
- **Response**: `cancelOpenIncidents` now uses `IncidentEventsService.appendEvent` for CANCELLED and RESOLVED demo-reset events, preserving hash-chain integrity.

### Issue 10 — Severity: suggestion
- **File**: apps/web/src/app/dashboard/responder/page.tsx:134
- **Description**: Responder panel never receives riskExplanation; only score/severity shown.
- **Suggestion**: Include riskExplanation on summary/detail APIs for responders.
- **Status**: fixed
- **Response**: Added `riskExplanation` to `IncidentSummary` type and `mapIncidentSummary`; responder dashboard passes it to `RiskExplanationPanel`.

### Issue 11 — Severity: nit
- **File**: apps/api/src/admin/admin.controller.ts:57
- **Description**: simulate-demo steps are not transactional; concurrent calls can race.
- **Suggestion**: Wrap in prisma.$transaction or add a lock.
- **Status**: fixed
- **Response**: Added `withDemoSimLock()` mutex and wrapped cancel/profile/responder updates in `prepareDemoScenario()` via `prisma.$transaction`.

### Issue 12 — Severity: nit
- **File**: apps/web/src/app/dashboard/operator/page.tsx:354
- **Description**: Row onClick bubbles from Acknowledge/Assign buttons.
- **Suggestion**: e.stopPropagation() on action buttons.
- **Status**: fixed
- **Response**: Added `e.stopPropagation()` to Acknowledge and Assign Responder button handlers.

### Issue 13 — Severity: nit
- **File**: apps/api/src/risk-scoring/risk-scoring.service.ts:39
- **Description**: Distance rule includes BUSY responders; Bible implies available nearest.
- **Suggestion**: Filter to AVAILABLE unless intentional.
- **Status**: fixed
- **Response**: Nearest-responder query now filters `availabilityStatus: 'AVAILABLE'` only.

---

## Fix Round Implementation Summary

**Commit:** `fix(phase6): address review findings`

### Changes by area

| Area | Fixes |
|------|-------|
| **API admin/demo** | Demo responder scoped by email; transactional `prepareDemoScenario`; mutex lock; proper audit events via `IncidentEventsService` |
| **API analytics** | Today's first-acknowledge response time average |
| **API risk scoring** | AVAILABLE-only responders; Asia/Kolkata night-time |
| **Shared** | `capRiskScore` export, boundary tests, documented max rule score (90) |
| **Web operator** | Debounced summary refresh on WebSocket; summary error banner; button stopPropagation |
| **Web responder** | `riskExplanation` on assignments |
| **Web components** | Parse-failure fallback in `RiskExplanationPanel` |

### Test Results

| Suite | Result |
|-------|--------|
| `npm run build:all` | ✅ Pass |
| `npm run test:shared` (11 tests) | ✅ Pass |
| `phase6-risk-analytics.spec.ts` (3 tests) | ✅ Pass |